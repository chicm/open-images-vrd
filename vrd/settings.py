import os.path as osp

ROOT_DIR = '/mnt/chicm/data/open-images'
DATA_DIR = osp.join(ROOT_DIR, 'relation')
DETECT_DATA_DIR = osp.join(ROOT_DIR, 'detect')
TRAIN_IMG_DIR = osp.join(ROOT_DIR, 'train', 'imgs')
TEST_IMG_DIR = osp.join(ROOT_DIR, 'test')
